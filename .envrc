# Load the nix flake development shell automatically
# This uses nix-direnv for better performance and garbage collection protection

if ! has nix_direnv_version || ! nix_direnv_version 3.2.0; then
  source_url "https://raw.githubusercontent.com/nix-community/nix-direnv/3.2.0/direnvrc" "sha256-hW6NC1JHue3IjZN3uDM6l6I2PMaauqd2D7hXYJ1Zfr4="
fi

# Use flake
use flake

# Optional: Watch for changes in these files and reload
watch_file flake.nix
watch_file flake.lock
watch_file modules/flake.nix
